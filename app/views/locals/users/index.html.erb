
<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>

<% @users.each do |user| %>
<h1> <%= link_to user.first_name, locals_user_path(user) %> </h1>
<h2> <%= user.age %> </h2>
<% end %>
